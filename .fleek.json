{
  "build": {
    "baseDir": "client",
    "publicDir": "build",
    "command": "apt-get update && apt-get install -y libusb-1.0-0 libusb-1.0-0-dev && yarn install --frozen-lockfile && yarn build",
    "image": "bitnami/node:10"
  }
}